const mathUtils ={

    // division
    division(x, y){
        return x / y;
    },

    // multiplication function
    multiply(a, b, c){
        return a * b * c;
    },

    // power of function
    power(k){
        return k * k;
    },

    //two variable
    pi : 3.142,
    version: "1.0.0"
    
};

module.exports = mathUtils;